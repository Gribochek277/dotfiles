-- ## Application Shortcuts ###

-- Note: Most app shortcuts are in main.conf

-- This file is reserved for additional app-specific bindings

-- Whisper Dictation (Super+V)
hl.bind("SUPER + V", hl.dsp.exec_cmd("~/.local/bin/whisper-dictate.sh"))
